[server]
hosts = ${EMITIMES__SERVER__HOST:-0.0.0.0}:${EMITIMES__SERVER__PORT:-36000}

[auth]
type = htpasswd
htpasswd_filename = data/.htpasswd

[storage]
filesystem_folder = data/storage/

[logging]
level = info
