[server]
hosts = 0.0.0.0:${EMITIMES_PORT:-36000}

[auth]
type = htpasswd
htpasswd_filename = .htpasswd
htpasswd_encryption = md5

[rights]
type = authenticated

[storage]
filesystem_folder = /app/data

[logging]
level = info
